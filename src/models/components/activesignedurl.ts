/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ActiveSignedUrl = {
  /**
   * Time-limited signed URL
   */
  signedUrl: string;
  /**
   * Expiration timestamp (Unix epoch)
   */
  expiresAt: number;
};

/** @internal */
export const ActiveSignedUrl$inboundSchema: z.ZodMiniType<
  ActiveSignedUrl,
  unknown
> = z.pipe(
  z.object({
    signed_url: types.string(),
    expires_at: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "signed_url": "signedUrl",
      "expires_at": "expiresAt",
    });
  }),
);

export function activeSignedUrlFromJSON(
  jsonString: string,
): SafeParseResult<ActiveSignedUrl, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ActiveSignedUrl$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ActiveSignedUrl' from JSON`,
  );
}
